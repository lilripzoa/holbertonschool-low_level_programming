#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - that returns the length of a string.
 * @s: length.
 * Return: the length of a string.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
