#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @a: return the result
 * @b: return the result
 * Return: the result.
 */

int add(int a, int b)
{

	return (a + b);

}
